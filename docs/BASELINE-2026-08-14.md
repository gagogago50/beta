# Baseline technique — 14 août 2026

## Source

- Dépôt : `https://github.com/senlinjun/NEk0`
- Révision : `f5497d12ed368f8f2969f15d7f1dfabe05434a22`
- Usage de cette copie : prototype personnel uniquement.

## Changements initiaux

1. Android minimum fixé explicitement à API 28.
2. Sauvegarde Android et transfert de données désactivés tant que l’identité TS3 et les mots de passe résident dans SharedPreferences.
3. Ajout de règles `data_extraction_rules` excluant toutes les données de cloud backup/device transfer.
4. Correction des fuites mémoire FFI Dart :
   - adresse, nickname, canal et mot de passe de `connect` sont libérés dans un `finally` ;
   - message de canal libéré dans un `finally` ;
   - détection explicite d’un pointeur de chaîne natif nul.
5. Ajout d’un plan détaillé et d’un avertissement de statut de licence.

## Vérification

- `git diff --check` : réussi.
- Rust stable installé dans le cache de l’environnement : `rustc 1.97.1`.
- `cargo check --locked` : compilation engagée avec succès jusqu’à la dépendance hôte `alsa-sys`.
- Blocage attendu : cette sandbox Linux ne fournit pas les en-têtes ALSA (`alsa.pc`). Le livrable visé est Android et devra être vérifié avec Android NDK 26 via `pre_build.py`.
- Flutter/Android SDK ne sont pas installés dans la sandbox actuelle ; `flutter analyze` et l’APK restent à exécuter dans un environnement Android complet.

## Prochaine modification prioritaire

Migrer `client_identity` et les mots de passe de serveurs hors de SharedPreferences vers un stockage soutenu par Android Keystore, avec migration des valeurs existantes.
